# note-city — Ledger

> The agent updates this file at the end of every session. `docs/PLAN.md` does not change; all status lives here.
> How to update: use `str_replace`, anchor on the last row of the table you are editing, add the new row right after it. Never rewrite the whole file. Escape `|` inside cell content.

Plan followed: docs/PLAN.md v1.0
Current phase: Phase 2 (done, P2.1–P2.5 all done) → Phase 3 (stretch)
Next task: P3.1 (editor works on transcribed notes, "Play synth of edited version"). PR #19 and PR #20 both merged; `main` synced.

---

## 1a. Task approval (do this before status moves to `doing`)

For each task, before starting implementation: copy its DoD from `docs/PLAN.md`, write a
Test Plan (exact commands / exact Minh checkpoint question), and record it here.
This is the approval gate — it exists so a task's scope and exit criteria are
committed *before* work starts, not reconstructed from the diff afterward. Don't
expand scope mid-task; log unrelated findings in `docs/BUGS.md`, `docs/PLAN.md` DR-N (via a
new decision), or LEDGER section 6 (parked ideas) instead of folding them in.

| ID | DoD (copied from docs/PLAN.md) | Test Plan (commands / checkpoint question) | Approved |
|---|---|---|---|
| P1.1 | `npm run dev` opens a blank page. `npm test` green. | `npm run dev` manual check; `npm test` | yes (2026-09-12) |
| P1.2 | Tests written first. Edge cases: midi outside 21..108, duration 0. Green. | `npm test` — new tests for `midiToFreq`, `midiToName`, `midiToHeight`, `durationToWidth` written and failing (red) before implementation, then passing (green). No Minh checkpoint (harness task). | yes (2026-09-12) |
| P1.3 | Pure scheduler tested. **Minh checkpoint:** press 2 buttons, hear 2 notes 12 steps apart — Minh says which is higher. | `npm test` — `schedule(notes, now)` tests written red-first (empty array, one note, several notes, `now` offset applied). `playNote`/AudioContext wiring is `[skill]`, no automated test. Minh checkpoint: two buttons in the running app (`npm run dev`), one plays midi 60 (C4) one plays midi 72 (C5, 12 semitones up) — Minh listens and says which button's note is higher, verbatim recorded in section 3. | yes (2026-09-12) |
| P1.4 | `[skill]`, no automated test (Canvas drawing). **Minh checkpoint:** look at 5 buildings, point to the tallest before listening, then listen to confirm. | `npm run dev` — render a fixed 5-note test set on `src/ui/skyline.ts`'s Canvas (vertical = midi via `midiToHeight`, horizontal = time via `durationToWidth`), with a playhead that advances during playback and hover showing the note name via `midiToName`. Minh points to the tallest building before playback, then plays and confirms by ear; verbatim recorded in section 3. | yes (2026-09-12) |
| P1.5 | `hitTest`/`snap*` pure and tested. **Minh checkpoint:** drag a building up 1 step, listen; up 12 steps, listen — describe the difference in Minh's words. | `npm test` — `snapMidi(midi)` and `snapTime(sec, stepSec)` written red-first (round-to-nearest-integer / round-to-nearest-step, edge cases at exact halfway and at range boundaries); `findNoteAt` already covered by P1.4. Canvas pointer-drag wiring (click empty space → add note; drag body vertically → change midi via `snapMidi`; drag right edge → change duration via `snapTime`; release → play that note) is `[skill]`, no automated test. Minh checkpoint: in `npm run dev`, drag one building up 1 semitone and listen, then drag the same building up 12 semitones (one octave) and listen, then describe the difference in Minh's own words; verbatim recorded in section 3. | yes (2026-09-12) |
| P1.8 | Tests written first, same edge cases as `midiToName`. **Minh checkpoint:** hover a few buildings, Minh says the label reads naturally to him (Đô Rê Mi..., not C D E). | `npm test` — `midiToSolfege(midi)` written red-first in `test/notes/mapping.test.ts`, same edge cases as the existing `midiToName` tests (each of the 12 pitch classes incl. sharps, octave boundaries, negative/high octaves). Hover-label wiring in `main.ts` to show solfège as the default name is `[skill]`, no automated test. Minh checkpoint: in `npm run dev`, hover a few buildings and confirm the label reads naturally (Đô4, Rê4, ...); verbatim recorded in section 3. | yes (2026-09-12) |
| P1.6 | `[skill]`, no automated test (seed data + button wiring). **Phase 1 final checkpoint:** Minh clears the seed, builds 8 notes, says "sounds okay", and explains 1 choice. Record verbatim in the ledger. | `npm run dev` — replace the 5-note test set in `main.ts` with the 8-note C-major-scale seed (60 62 64 65 67 69 71 72), add a "Clear all" button (empties `notes`, re-renders) alongside the existing "Play" button. No new pure functions, so no new automated test. Minh checkpoint: in the running app, Minh presses "Clear all", builds his own 8-note melody using the P1.5 editor (click to add, drag to tune), presses Play, says whether it "sounds okay", and explains (in his own words) why he chose one particular note; verbatim recorded in section 3. | yes (2026-09-12) |
| P1.9 | `[skill]`, no automated test (static note data + button wiring). **Minh checkpoint:** Minh plays each of the 3 presets and says whether it sounds pleasant/recognizable, not just "notes playing". | `npm run dev` — add 3 buttons next to the P1.6 seed/Clear/Play controls, each loading a well-known simple tune (Twinkle Twinkle Little Star, Mary Had a Little Lamb, Ode to Joy's opening phrase) into `notes` and playing it. No new pure functions, so no new automated test. Minh checkpoint: press each preset button, listen, and say whether each one sounds pleasant/recognizable; verbatim recorded in section 3. | yes (2026-09-12) |
| P1.7 | `[skill]`, no automated test (layout/type/color/spacing only). **Minh checkpoint:** looking at the page, Minh says it reads as a considered design, not the default Vite scaffold look, and every existing control (buttons, canvas, hover label) still works exactly as before. | `npm run dev` — use the `/frontend-design:frontend-design` skill for aesthetic direction; restyle `src/style.css`/`index.html`/`main.ts`'s markup (layout, typography, color, spacing) with no functional changes — every button id, the canvas, hitTest/drag coordinates, and the hover label keep working exactly as before. `npm test` must stay green (regression guard: no pure function touched). Minh checkpoint: open the running app, confirm it reads as a considered design (not default Vite scaffold), then exercise every control (Play C4/C5, click-to-add, drag pitch/duration, Clear all, Play, 3 presets, hover label) and confirm nothing broke; verbatim recorded in section 3. | yes (2026-09-12) |
| P1.10 | Minh can build a full melody (8+ notes at varied durations) without hitting an invisible/unreachable edge. **Minh checkpoint:** Minh tries to place and stretch a note near the right edge and confirms it no longer clips or becomes ungrabbable. | `npm test` unaffected (no new pure function — the clamp reuses the already-tested `xToTime`). Widen `#skyline`'s canvas from 500px/5s to 700px/7s; in `main.ts`, clamp `addNoteAt`'s computed `start` and the duration-drag's cap so a note's `[start, start+duration)` can never extend past the canvas's own width. Minh checkpoint: in `npm run dev`, place a note near the new right edge and drag its right edge as far as it goes, confirm it stays fully visible and grabbable the whole time; verbatim recorded in section 3. | yes (2026-09-12) |
| P1.11 | `pushHistory`/`undo` tested (edge cases: empty stack, undo past the start). **Minh checkpoint:** Minh places/drags a few notes and confirms feedback feels immediate; presses undo after a "Clear all" and gets the melody back; drags a note to the right edge and sees a clear stop-cue instead of it just refusing to move. | `npm test` — `src/ui/history.ts` (`pushHistory(stack, state)`, `undo(stack)`, pure) written red-first in `test/ui/history.test.ts`; edge cases: undo on an empty stack (no-op / returns undefined, doesn't throw), undo past the start (stays at the oldest state, doesn't underflow). Wired into `main.ts` so every add/drag/delete/"Clear all" pushes a snapshot of `notes` first, and a new "Undo" button pops one. Parts (a) instant micro-feedback (a visible "pop" on placement, live pitch/duration readout while dragging) and (c) edge-drag stop-cue (visible highlight when a drag hits the P1.10 canvas boundary) are `[skill]`, no automated test — Canvas/CSS feel only, layered on existing P1.4/P1.5/P1.10 hitTest/drag/clamp mechanics. Minh checkpoint: in `npm run dev`, (a) place/drag a few notes and confirm feedback feels immediate; (b) press "Clear all" then "Undo" and confirm the melody comes back; (c) drag a note to the right edge and confirm a clear visual stop-cue appears instead of the drag just silently refusing to move further; verbatim recorded in section 3. | yes (2026-09-14) |
| P2.1 | `src/audio/decode.ts`: `<input type=file>` accepts audio + video → `decodeAudioData` → `toMono(buffer)` → `resample(buffer, 22050)` (OfflineAudioContext). | `npm test` — `toMono(channels: Float32Array[]): Float32Array` written red-first in `test/audio/decode.test.ts`, tested with a synthetic 2-channel buffer (averages sample-wise; edge cases: 1-channel passthrough, silent/zero channels). `resample` is exempt from automated TDD per DR-11 (thin `OfflineAudioContext` wrapper, no Node/vitest Web Audio support) — verified manually: same length in seconds before/after, output `sampleRate === 22050`. File-input wiring (`<input type=file>` → `decodeAudioData` → `toMono` → `resample`) is `[skill]`/manual: mp3, wav, m4a, mp4 opened in Chrome via `npm run dev`, confirmed no console error and a decoded buffer logged. No formal Minh checkpoint question beyond "these 4 files open without error" — Phase 2's real checkpoint is P2.5. | yes (2026-09-17) |
| P2.2 | `[skill]`, no code. **Minh checkpoint:** Minh follows the doc and gets a wav file from a YouTube link. | No `npm test` impact (docs-only, no code touched). Write `docs/input.md`: step-by-step procedure for `youtube-mcp download_audio` (link → downloaded audio file) plus the ffmpeg fallback command from `docs/PLAN.md` section 5 (`ffmpeg -i input.<ext> -ac 1 -ar 22050 -t 60 output.wav`) for when a format doesn't decode in-browser. Minh checkpoint: Minh picks a real YouTube link, follows the doc's steps only (no agent hand-holding beyond what's written), and confirms he ends up with a playable wav file; verbatim recorded in section 3. | yes (2026-09-17) |
| P2.3 | `src/transcribe/basicPitch.ts`: add `@spotify/basic-pitch` + tfjs. `transcribe(pcm) → RawNote[]`. Contract test: 440 Hz sine, 1 s → a RawNote with midi 69, duration 0.8–1.2 s. Test 2: two sines in sequence (440 then 880) → 2 notes, midi 69 then 81, in order. | `npm test` — both contract tests written red-first in `test/transcribe/basicPitch.test.ts`, using `makeSine` (already built at P2.1) to generate PCM in code, no committed audio fixtures. Confirmed via Advise (`claude-opus-5`, empirically ran `@spotify/basic-pitch` under plain Node before recommending) that `evaluateModel` accepts a raw `Float32Array` directly (no fake `AudioBuffer` needed) and tfjs's `cpu` backend works in vitest/Node — so this is genuinely TDD'able, no DR-11-style exemption. `transcribe()` takes an injectable model source (defaults to the app's model URL for the browser; tests inject a Node `fs`-based `tf.io.IOHandler` reading `node_modules/@spotify/basic-pitch/model/`). Pin top-level `@tensorflow/tfjs` to `^3.21.0` to match basic-pitch's peer dependency and avoid a duplicate nested copy in the Vite bundle. | yes (2026-09-17) |
| P2.4 | `src/notes/clean.ts`: `filterShort(notes, minDur)`, `filterLowConfidence(notes, min)`, `mergeAdjacent(notes, gap)` — same midi, gap smaller than threshold → merge. | `npm test` — all three functions written red-first in `test/notes/clean.test.ts`. Per RETRO-1, also check against `docs/PLAN.md` section 1 non-goals (single melody, one note at a time) and section 4 data contract while editing `RawNote[]`/`Note[]`. Edge cases: empty array (all 3 fns), single note (no merge partner), two notes with the same `start` (merge boundary — `mergeAdjacent`'s `gap` is the silence between `a.start + a.duration` and `b.start`; same-`start` notes have a negative/zero gap, so they must merge if same midi), `minDur`/`min` confidence boundary values (exactly at threshold — keep, per PLAN.md's usual "raise the threshold" framing meaning strictly-below is filtered), `mergeAdjacent` preserving order and not mutating input (matches the no-mutation convention from `scheduler.ts`/P1.3). No `[skill]`/Minh checkpoint — pure data functions, not wired into the UI yet (P2.5 wires them into the skyline). | yes (2026-09-17) |
| P2.5 | Wire: file → decode → transcribe → clean → skyline. Toggle "play original" / "play synth". Playhead synced to the original. **Phase 2 final checkpoint:** Minh loads a clean clip ≤ 60 s, points to at least 1 place where the skyline matches the ear and 1 where it doesn't (if any); record both. | `[skill]` overall (wiring, not a new data-transform), reusing already-TDD'd `decodeFile` (P2.1), `transcribe` (P2.3), `filterShort`/`filterLowConfidence`/`mergeAdjacent` (P2.4) as-is with fixed default thresholds (`minDur=0.05s`, `minConfidence=0.3`, `mergeGap=0.05s` — tunable later if the checkpoint shows notes too sparse/noisy, per `docs/PLAN.md`'s "raise the threshold, don't touch the model" guidance). One new pure function, following the established `src/ui/` precedent (geometry/hitTest/snap/history were all TDD'd even though not literally named in the hard-rule-3 list): `fitSkylineOptions(notes, canvasWidth, canvasHeight) → SkylineOptions` in `src/ui/geometry.ts`, computing a `midiRange` (padded around the notes' min/max midi, floor 12-semitone span, clamped to piano range 21..108) and `pxPerSec` (canvas width / total duration, clamped to a sane min/max) so a real transcribed melody of unknown pitch range/duration fits the fixed 700×200 canvas — written red-first in `test/ui/geometry.test.ts`, edge cases: empty notes array (no divide-by-zero), single note, notes spanning less than the minimum span, notes spanning more than the canvas's clamped pxPerSec range. Model asset wiring: `@spotify/basic-pitch`'s model files (`node_modules/@spotify/basic-pitch/model/*`, ~900KB) aren't committed (not source, and matches the "don't commit binary fixtures" spirit) — a `scripts/copy-model.mjs` copies them to `public/basic-pitch/model/` (gitignored) via `predev`/`prebuild` npm hooks, matching `DEFAULT_MODEL_URL` from P2.3. `npm test` must stay green; `tsc --noEmit`/`npm run build` clean (build now depends on the copy script having run — verify `npm run build` alone, from a clean `public/basic-pitch/`, still produces a working `dist/`). Minh checkpoint (Phase 2 final): load a real ≤60s clip (reuse a YouTube-sourced wav from P2.2, or the mp3 already used at P2.1) through the Import section, confirm the skyline populates, press "Play original audio" and "Play synth" and compare, then point to 1 matching spot and 1 non-matching spot (if any); verbatim recorded in section 3. | yes (2026-09-17) |
| P2.6 | Import UX polish (DR-13, BUG-6), 2 parts: (a) real progress/error feedback during decode → transcribe → clean; (b) reflow so the skyline canvas reads as the primary element. | `npm test` — new test in `test/transcribe/basicPitch.test.ts` confirming `transcribe(pcm, model, onProgress)` calls `onProgress` with values reaching 1.0 (wires Basic Pitch's existing `percentCallback`, previously discarded as `() => {}`); existing 2 contract tests stay green, no change to `transcribe`'s return shape. `[skill]` for the rest (spinner/error-banner wiring in `main.ts`, HTML/CSS reflow in `index.html`/`src/style.css`) — no automated test, verified by Minh. `tsc --noEmit`/`npm run build` must stay clean. Minh checkpoint: (a) import a real clip, confirm he can tell decoding/transcribing-with-moving-%/done/failed at every moment without guessing; (b) looking at the page, confirm the skyline reads as the main focus over the header/Play C4/C5 buttons. | yes (2026-09-17) |

---

## 1. Task status

Status: `todo` | `doing` | `blocked` | `done`. "Actual" is counted in sessions. "Evidence" is a commit hash, a test path, or Minh's words (verbatim, short).

| ID | Status | Est. | Actual | Evidence | Done on |
|---|---|---|---|---|---|
| P1.1 | done | 1 | 1 | `npm test` green (1 test), `npm run dev` served 200 on :5173 | 2026-09-12 |
| P1.2 | done | 1 | 1 | `npm test` green (16 tests: mapping.test.ts + smoke.test.ts) | 2026-09-12 |
| P1.3 | done | 1 | 1 | `npm test` green (21 tests: scheduler.test.ts + mapping.test.ts + smoke.test.ts); checkpoint passed, see section 3 | 2026-09-12 |
| P1.4 | done | 1–2 | 1 | `npm test` green (32 tests: geometry.test.ts + hitTest.test.ts + scheduler.test.ts + mapping.test.ts + smoke.test.ts); checkpoint passed, see section 3 | 2026-09-12 |
| P1.5 | done | 2 | 1 | `npm test` green (59 tests: overlap.test.ts + snap.test.ts + geometry.test.ts + hitTest.test.ts + scheduler.test.ts + mapping.test.ts + smoke.test.ts); checkpoint passed, see section 3 | 2026-09-12 |
| P1.6 | done | 0.5 | 1 | `npm test` green (65 tests, unchanged — no new pure functions); checkpoint passed, see section 3 | 2026-09-12 |
| P1.8 | done | 0.5 | 1 | `npm test` green (38 tests: mapping.test.ts + geometry.test.ts + hitTest.test.ts + scheduler.test.ts + smoke.test.ts); checkpoint passed, see section 3 | 2026-09-12 |
| P1.9 | done | 0.5 | 1 | `npm test` green (65 tests, unchanged — no new pure functions); checkpoint passed, see section 3 | 2026-09-12 |
| P1.7 | done | 1 | 1 | `npm test` green (65 tests, unchanged); checkpoint passed after 2 rounds of visual iteration, see section 3 | 2026-09-12 |
| P1.10 | done | 0.5 | 1 | `npm test` green (65 tests, unchanged); checkpoint passed, see section 3 | 2026-09-12 |
| P1.11 | done | 1 | 1 | `npm test` green (71 tests: history.test.ts + existing suites); checkpoint passed, see section 3 | 2026-09-17 |
| P2.1 | done | 1–2 | 1 | `npm test` green (75 tests: decode.test.ts + existing suites); `tsc --noEmit`/`npm run build` clean; verified in Chrome — synthetic WAV (1.00s @ 22050 Hz mono) and a real mp3 ("LAVIEM.mp3", 270.62s → 5,967,171 samples @ 22050 Hz mono, exact) — see session log for m4a/mp4 caveat | 2026-09-17 |
| P2.2 | done | 0.5 | 1 | `docs/input.md` written; verified by executing the procedure with a real YouTube link (`download_audio` → wav), Minh confirmed the file plays; checkpoint passed, see section 3 | 2026-09-17 |
| P2.3 | done | 2 | 1 | `npm test` green (80 tests: basicPitch.test.ts + existing suites); `tsc --noEmit`/`npm run build` clean; Grade (`claude-haiku-4-5`) independently checked the diff against the DoD, PASS | 2026-09-17 |
| P2.4 | done | 1 | 1 | `npm test` green (100 tests: clean.test.ts + existing suites); `tsc --noEmit`/`npm run build` clean | 2026-09-17 |
| P2.5 | done | 1–2 | 2 | `npm test` green (108 tests: geometry.test.ts additions + existing suites); `tsc --noEmit`/`npm run build` clean; BUG-4 (no stop) and BUG-5 (no sound on long clips) found and fixed during the checkpoint; Phase 2 final checkpoint passed with a real ~270s song ("LAVIEM.mp3"), see section 3 | 2026-09-17 |
| P2.6 | done | 1 | 1 | `npm test` green (109 tests: basicPitch.test.ts onProgress test + existing suites); `tsc --noEmit`/`npm run build` clean; checkpoint passed (both parts), see section 3 | 2026-09-17 |
| P3.1 | todo | 1 | | | |
| P3.2 | todo | 1 | | | |
| P3.3 | todo | 1–2 | | | |

---

## 2. Session log

One row per session. "Result" is the state at the end of the session, not a plan. "Next" is one concrete action for the following session.

| Date | Session | Tasks touched | Result | Variance vs estimate and why | Next |
|---|---|---|---|---|---|
| 2026-09-11 | 0 | — | Plan v1.0 and ledger created. No code yet. | — | P1.1: scaffold Vite + TS + vitest, smoke test green, commit |
| 2026-09-12 | 1 | P1.1 | git repo initialized; scaffolded Vite vanilla-ts + vitest in project root (via temp subdir to avoid clobbering PLAN.md/LEDGER.md/CLAUDE.md/BUGS.md/RETRO.md); `npm test` (1 smoke test) and `npm run dev` both verified. Way-of-working docs (approval gate, bug/retro logs) also bootstrapped this session. | On estimate (1 session) | P1.2: `src/notes/` types and pure functions (midiToFreq, midiToName, midiToHeight, durationToWidth), tests first |
| 2026-09-12 | 2 | P1.2 | Added `src/notes/types.ts` (Note, RawNote, Project, MidiRange per PLAN.md section 4) and `src/notes/mapping.ts` (midiToFreq, midiToName, midiToHeight, durationToWidth). Tests written first in `test/notes/mapping.test.ts`, seen red (module missing), then green after implementation. Edge cases covered: midi outside 21..108 range clamps to 0/1 in midiToHeight, duration 0 gives width 0. `npm test` green (16 tests), `tsc --noEmit` clean. | On estimate (1 session) | P1.3: `src/audio/synth.ts` (playNote) and `src/audio/scheduler.ts` (schedule), pure scheduler tested first; then Minh checkpoint (2 notes 12 steps apart) |
| 2026-09-12 | 3 | P1.3 | Added `src/audio/scheduler.ts` (`schedule(notes, now)`, pure), tests written first in `test/audio/scheduler.test.ts` (empty array, single note, `now` offset, multiple notes, no-mutation), seen red then green. Added `src/audio/synth.ts` (`playNote` with OscillatorNode + linear attack/release gain envelope, `[skill]`, no automated test). Replaced the Vite scaffold `main.ts`/`index.html` boilerplate with a minimal playground: two buttons (Play C4 / Play C5) wired through `schedule` + `playNote`. Removed now-unused `src/counter.ts`. Ran `npm run dev`, Minh pressed both buttons. `npm test` green (21 tests), `tsc --noEmit` and `npm run build` clean. Between sessions, also: PR #1 (P1.2) and PR #2 (docs → `docs/`) merged; added a new (not plan-listed) CI feature — `.github/workflows/ci.yml` running typecheck+test+build on Node 22, PR #3 merged, branch protection on `main` now requires it (recorded as DR-6 in `docs/PLAN.md`). | On estimate (1 session for P1.3); CI setup was an unplanned but requested addition, tracked outside the phase estimate | P1.4: `src/ui/skyline.ts` — draw `Note[]` as buildings on Canvas, playhead during playback, hover shows note name; Minh checkpoint (point to tallest building) |
| 2026-09-12 | 4 | P1.4 | Added `src/ui/geometry.ts` (`noteRect`, pure — bottom-anchored bar geometry from midi/duration) and `src/ui/hitTest.ts` (`findNoteAt`, pure — point-in-rect over notes, topmost wins), both tests written first (red → green) in `test/ui/geometry.test.ts` and `test/ui/hitTest.test.ts`. Added `src/ui/skyline.ts` (`drawSkyline`, Canvas 2D drawing + playhead line, `[skill]`, no automated test). Wired into `main.ts`: a 5-note non-monotonic test set, hover shows note name via `midiToName`, a Play button animates the playhead with `requestAnimationFrame` against `AudioContext.currentTime`. `npm test` green (32 tests), `tsc --noEmit` and `npm run build` clean. Ran `npm run dev`; Minh checkpoint passed. | On estimate (1 session, within the 1–2 estimate) | P1.5: editor — click empty space to add a building, drag vertically to change midi (snap to integer), drag right edge to change duration; `hitTest`/`snapMidi`/`snapTime` pure and tested; Minh checkpoint (drag 1 step vs 12 steps) |
| 2026-09-12 | 5 | — | No code this session. At Minh's request, added task P1.7 (frontend redesign of the Phase 1 playground using the `/frontend-design:frontend-design` skill) to `docs/PLAN.md`'s Phase 1 table, depending on P1.6; recorded as DR-7. Added the row to the task status table below (`todo`). Not started — P1.5 and P1.6 still come first. | — (plan amendment, not an estimated task) | P1.5: editor — click empty space to add a building, drag vertically to change midi (snap to integer), drag right edge to change duration; `hitTest`/`snapMidi`/`snapTime` pure and tested; Minh checkpoint (drag 1 step vs 12 steps) |
| 2026-09-12 | 6 | P1.5 | Added `src/ui/snap.ts` (`snapMidi`, `snapTime`) and `src/ui/geometry.ts` additions (`yToMidi`, `xToTime`, inverse of `noteRect`'s mapping), tests written first in `test/ui/snap.test.ts` and `test/ui/geometry.test.ts`, red → green. Wired an editor into `main.ts`: click empty canvas space adds a building (pitch/time from the click point, snapped); dragging a building's body changes its pitch live; dragging its right edge changes its duration live; release plays the edited note. Minh's first manual pass found a real bug — added buildings could overlap an existing one in time, effectively creating a chord, which violates the "single melody, one note at a time" non-goal in `docs/PLAN.md` section 1. Fixed within the same task (not deferred to `docs/BUGS.md`, since it was found before the task was marked done and was part of the feature just built): added `src/notes/overlap.ts` (`timeRangesOverlap`, `overlapsAny`, `maxDurationAt`), tests first, red → green; wired so an overlapping add is a no-op and a duration drag is capped at the next note's start. Logged the generalizable lesson as `docs/RETRO.md` RETRO-1 (check `docs/PLAN.md` section 1 invariants, not just the task's DoD line, whenever editing `Note[]`). `npm test` green (59 tests), `tsc --noEmit` and `npm run build` clean. Ran `npm run dev`; Minh checkpoint passed (see section 3). | On estimate (1 session, within the 2-session estimate) | P1.6: seed — preload the 8-note C-major-scale melody, "Clear all" and "Play" buttons; Phase 1 final checkpoint (Minh builds 8 notes by hand, says "sounds okay", explains 1 choice) |
| 2026-09-12 | 7 | P1.8 | Minh asked for note labels in Vietnamese solfège instead of letter names, mid-checkpoint of P1.5. Answered open question Q3, recorded as DR-8, added task P1.8 to `docs/PLAN.md` (branched off `main` rather than `p1.5-editor`, since PR #7/P1.5 was still open — kept as its own branch/PR, `p1.8-solfege-names`, to avoid duplicating P1.5's unmerged content; merged both into this branch via `git merge origin/main` once PR #7 landed). Added `midiToSolfege` (fixed-do: Đô=C, Rê=D, ...) to `src/notes/mapping.ts`, tests written first in `test/notes/mapping.test.ts` (same edge cases as `midiToName`, plus all 12 pitch classes), red → green. Wired into `main.ts`'s hover label to show `"Đô4 (C4)"` (solfège default, letter name alongside) — after the merge, on top of P1.5's editor (`notes`, `pointFromEvent`, click/drag wiring). Also logged a parked idea (constrain the editor to a musical scale) — recommended against for v1, not built. `npm test` green, `tsc --noEmit` and `npm run build` clean. Ran `npm run dev`; Minh checkpoint passed ("Có, chính xác rồi."). | On estimate (1 session, within the 0.5 estimate — rounds up since it's a discrete session) | P1.6: seed the 8-note C-major-scale melody, "Clear all" and "Play" buttons; Phase 1 final checkpoint |
| 2026-09-12 | 8 | P1.6, P1.9 | Replaced the P1.4 5-note test set in `main.ts` with the P1.6 seed (8-note C-major scale, 60 62 64 65 67 69 71 72), added a "Clear all" button. Minh's checkpoint pass surfaced audio crackling during rapid editing, logged as `docs/BUGS.md` BUG-1 (not fixed — out of P1.6's scope, root cause unconfirmed) and a design question about a fixed slot grid vs. free duration, answered and recorded as `docs/PLAN.md` DR-9 (kept free duration, logged the grid idea as parked) plus a new task P1.9 (3 pre-made example melodies) which Minh asked for in the same message. Implemented P1.9: extracted `playAll()` from the Play button's handler (also guards against playing an empty `notes` array, a latent edge case the Clear button exposed), added `loadMelody(midis)` and 3 preset buttons (Twinkle Twinkle Little Star, Mary Had a Little Lamb, Ode to Joy's opening phrase). No new pure functions in either task, so no new automated tests; `npm test` still green (65 tests, unchanged), `tsc --noEmit` and `npm run build` clean. Ran `npm run dev`; both checkpoints passed (see section 3) — P1.6's "explain 1 choice" answer was that Minh clicked mostly at random and found deciding notes hard, itself useful signal tying back to the grid-vs-free-duration discussion. Phase 1's final checkpoint (P1.6) is done; P1.7 (frontend redesign) is the only Phase 1 task left. | On estimate (1 session combined; P1.9 wasn't separately estimated, folded into the same session as P1.6) | P1.7: frontend redesign of the Phase 1 playground using the `/frontend-design:frontend-design` skill |
| 2026-09-12 | 9 | P1.7 | Used the `/frontend-design:frontend-design` skill: a "city at night" direction (buildings are lit windows, literally tying the visual to the note-city metaphor) — deep navy palette, condensed "Big Shoulards Display" wordmark (evokes skyscrapers), IBM Plex Sans/Mono for body/readout, single left-aligned column. Restyled `index.html` (Google Fonts), `src/style.css` (full rewrite, dropped unused Vite-scaffold styles/assets), and `main.ts`'s markup (same element ids, no functional change). Verified with the browser tool: click-to-add/drag/hover all still worked (one false alarm — the browser tool's screenshot coordinate frame didn't match the real viewport, not an app bug; confirmed via a direct DOM-dispatched click). Minh's first pass on the restyle asked whether to add a library like Three.js for a less "cùi bắp" (crude) look — recommended against (Canvas 2D is enough, "subtract before add"), then found two real issues instead: (1) windows didn't read as windows and the flat dark background didn't read as a city — iterated `src/ui/skyline.ts` twice (a first window-grid attempt looked like a checkerboard texture; fixed a hash bug where the row coefficient was a multiple of the modulus, canceling row variation into solid vertical lines; settled on a faint window-frame grid with sparse lit windows, plus a sky gradient and a ground strip drawn before the buildings) — Minh accepted the result as "better, though not fully polished" and asked to park further sky polish; (2) BUG-2 (right-edge clipping, see below) reproduced again — Minh decided to fix it as a dedicated task (P1.10) right after P1.7 rather than deferring further. `npm test` stayed green (65 tests, unchanged) throughout; `tsc --noEmit` and `npm run build` clean. Phase 1 (P1.1–P1.9) is now fully done. | On estimate (1 session, within the 1-session estimate, despite 2 rounds of visual iteration) | P1.10: fix BUG-2 (widen the canvas, clamp add/drag to the visible edge) |
| 2026-09-12 | 10 | P1.10 | Fixed BUGS.md BUG-2: widened `#skyline` from 500px/5s to 700px/7s; in `main.ts`, `addNoteAt`'s computed `start` and the duration-drag's cap are now clamped (via a new `maxVisibleTime()` helper built on the already-tested `xToTime`) so a note's `[start, start+duration)` can never extend past the canvas's own width. Branched off `main` (pre-P1.7) since PR #10 wasn't merged yet, then merged `origin/main` in once it landed, resolving conflicts in `main.ts`/`style.css`/`docs/LEDGER.md`/`docs/BUGS.md` (kept P1.7's markup/styling, P1.10's wider canvas and clamps; widened `.wrap` to 760px so the 700px canvas fits without forcing scroll on typical desktop widths). Verified via dispatched DOM events before asking Minh: a note placed near the new right edge renders fully, dragging further right stays clamped. `npm test` green (65 tests, unchanged — no new pure function, the clamp composes already-tested `xToTime`), `tsc --noEmit` and `npm run build` clean. Minh checkpoint passed ("ok rồi"). Phase 1 (P1.1–P1.10) is now fully done, including the bug found during its own checkpoints. | On estimate (1 session) | Phase 2: P2.1 — `src/audio/decode.ts` (file input → decodeAudioData → toMono → resample to 22050 Hz) |
| 2026-09-14 | 11 | — | No code this session. Agent brainstormed 3 playground UX ideas post-P1.10 (instant micro-feedback, in-memory undo, edge-drag stop-cue), parked in section 6. Minh then asked to scope all 3 into the next task. Added task P1.11 to `docs/PLAN.md`'s Phase 1 table, depending on P1.10; recorded as DR-10. Added the row to the task status table below (`todo`), updated the 3 parked-idea rows in section 6 to point at P1.11. Not started — this was scoping only, no approval-gate row in section 1a yet (that happens when P1.11 is actually picked up). | — (plan amendment, not an estimated task) | P1.11: playground interaction polish (micro-feedback, undo, edge stop-cue); or P2.1 if Minh prefers Phase 2 first |
| 2026-09-14 | 12 | — | No code this session. PR #12 (P1.11 scoping) confirmed merged; `main` pulled. Ran `/bootstrap-way-of-working` re-calibration at Minh's request before starting P1.11: contributors still solo, blast radius still low, no re-calibration trigger from `CLAUDE.md` fired — Tier 2 (current setup) confirmed still correct, no new artifact installed. Flagged one candidate for later (session log entries in this section getting long/verbose, ~150 lines after 11 sessions) — Minh chose not yet; revisit if it gets harder to scan. | — (calibration check, not an estimated task) | P1.11: branch off `main`, approval-gate row in section 1a, then TDD for `src/ui/history.ts` |
| 2026-09-17 | 13 | P1.11 | Confirmed no code needed writing this session — PR #13 (`p1.11-playground-polish`) already had the full implementation (`src/ui/history.ts` push/undo tested first, Undo button wired to add/drag/delete/Clear all/preset-load, pop-ring + live drag readout + edge stop-cue, plus BUG-1 and BUG-3 fixes found during the checkpoint) from a prior session, open and CLEAN/MERGEABLE against `main`. Checked out `p1.11-playground-polish`, pulled 3 commits it was behind, ran `npm run dev`, Minh exercised micro-feedback/undo/edge stop-cue plus the two pending bugfixes and confirmed. `npm test` green (71 tests). Marked P1.11 `done` in section 1, recorded Minh's checkpoint words in section 3, closed BUG-1 and BUG-3's "awaiting confirmation" state in `docs/BUGS.md`. | On estimate (1 session; work itself was done in a previous session, this session was verification + checkpoint) | Merge PR #13 once Minh gives the go-ahead, then `git checkout main && git pull` before branching for P2.1 |
| 2026-09-17 | 14 | P2.1 | PR #13 merged, local `main` synced. Hit a real ambiguity before coding: the hard TDD rule and P2.1's DoD both list `resample` as needing tests-before-implementation, but the DoD also names `OfflineAudioContext` (a browser API with no Node/vitest support, and jsdom/polyfills aren't on the dependency allow-list) as its mechanism. Fired Advise (`claude-opus-5`); recommendation was to keep `OfflineAudioContext` (a hand-rolled resampler would alias content above the new Nyquist frequency, corrupting P2.3's Basic Pitch input) and exempt `resample` from automated TDD as a thin, manually-verified wrapper — recorded as DR-11 in `docs/PLAN.md`. Added the P2.1 approval-gate row to section 1a — first attempt landed the commit directly on `main` before branching, caught immediately (nothing pushed yet), reset local `main` to `origin/main` and re-applied the commit only on the new branch `p2.1-decode-audio`. Built `src/audio/decode.ts`: `toMono(channels: Float32Array[])` (pure, TDD'd red-to-green in `test/audio/decode.test.ts` — 2/3+/1-channel averaging, empty-channels edge case), `resample` (OfflineAudioContext wrapper per DR-11), `decodeFile` (File → decodeAudioData → toMono → resample). Wired a minimal `<input type=file>` "Import audio (P2.1 test)" section into `main.ts` — decode-only, reports duration/sample count/rate, deliberately not wired into the skyline (that's P2.5, avoiding scope growth). `npm test` green (75 tests), `tsc --noEmit`/`npm run build` clean. Manually verified in Chrome with a synthetic 1s/44.1kHz stereo WAV generated in a scratch script (not committed, per "never commit audio fixtures"): decoded to `1.00s, 22050 samples @ 22050 Hz mono` — correct. Did **not** verify mp3/m4a/mp4 as the DoD's Test Plan asks — no ffmpeg available in this environment and no real sample files on hand to synthesize or source them; `decodeAudioData`'s codec support is a browser capability the app code doesn't touch differently per format, but this was a real gap against the written Test Plan, flagged here rather than silently claimed. Minh closed part of it same-day: tried a real mp3 ("LAVIEM.mp3") through the Import section, got `270.62s, 5967171 samples @ 22050 Hz mono` — 270.62 × 22050 = 5,967,171 exactly, confirming duration is preserved and the sample count is correct for a real, non-synthetic file. m4a and mp4 remain unverified. Minh then asked to commit that mp3 as a repo sample for future testing — declined per `docs/PLAN.md` section 7 / `CLAUDE.md` ("never commit audio fixture files"; fixtures are generated in code), and the file was never on this machine anyway (uploaded from Minh's own browser session straight into the page). Built the alternative instead: `test/fixtures/makeSine.ts` (`makeSine(freqHz, seconds, sampleRate)`, deterministic sine PCM), the helper `docs/PLAN.md` section 7 already names for P2.3's Basic Pitch contract tests — pulled it in one task early since P2.1 is where audio-fixture testing started. Tests first in `test/fixtures/makeSine.test.ts` (red → green): length matches `seconds * sampleRate`, bounded to [-1, 1] and starts at phase 0, and a frequency that divides the sample rate evenly (50 Hz @ 22050) produces the expected count of rising zero-crossings. `npm test` green (78 tests). | On estimate (1 session, within 1–2) | m4a/mp4 verification still open, low priority (codec support is a browser capability, mp3+wav already confirm the app-side logic); P2.3 — `src/transcribe/basicPitch.ts`, reusing `makeSine`|
| 2026-09-17 | 15 | P2.2 | Branched `p2.2-input-doc` off `main`. Wrote `docs/input.md` (no code): YouTube link → `youtube-mcp download_audio` procedure, 429 retry cadence (8s/10s/15s), the ffmpeg fallback command from `docs/PLAN.md` section 5, and clip-choosing guidance for Phase 2 transcription quality. `npm test` unaffected (78 tests, unchanged — docs-only). Executed the checkpoint live: gave Minh a real YouTube link, ran `download_audio` with `format: wav` per the doc's steps, got `KARINA 카리나 '16 Bit' MV.wav` in one pass (no rate-limit hit, so the retry-cadence path wasn't exercised this time). Minh confirmed the file plays; on the doc's clarity he said it doesn't matter whether *he* personally understands the steps, only that the agent executed them successfully — a slightly different bar than the DoD's literal wording ("Minh follows the doc"), noted here rather than silently reinterpreted. | On estimate (0.5 session, rounds up to 1) | P2.3 — `src/transcribe/basicPitch.ts` (`@spotify/basic-pitch` + tfjs), contract tests with `makeSine` |
| 2026-09-17 | 16 | P2.3 | Branched `p2.3-basic-pitch` off `main`. Before coding, hit a real ambiguity: `@spotify/basic-pitch`'s README/types show `evaluateModel()` taking a Web Audio `AudioBuffer`, which looked like the same browser-only wall P2.1 hit with `OfflineAudioContext` (DR-11) — risking extending that exemption to the whole transcription layer. Fired Advise (`claude-opus-5`), which empirically installed the package and ran it under plain Node before answering: the real signature is `AudioBuffer \| Float32Array`, so a raw PCM `Float32Array` works directly — no exemption needed, fully TDD-able as `docs/PLAN.md` section 7 originally intended. Advise also supplied a working Node model-loading approach (a hand-written `tf.io.IOHandler` reading the package's bundled `model.json`/`.bin` off disk) and flagged a tfjs version mismatch to pin. Implemented `src/transcribe/basicPitch.ts` (`transcribe(pcm, modelSource?) → RawNote[]`, injectable model source defaulting to a browser URL) and `test/transcribe/basicPitch.test.ts` (two contract tests using `makeSine`, model loaded once via the fs `IOHandler` in `beforeAll`). Pinned `@tensorflow/tfjs` to `^3.21.0` to match basic-pitch's peer dependency (`npm ls` confirms single deduped copy). First test run: the single-sine test passed but the two-sine order test failed (notes came back midi 81 before 69) — Basic Pitch's raw output isn't chronologically sorted; fixed with an explicit `.sort((a,b) => a.startTimeSeconds - b.startTimeSeconds)` in `transcribe()`, both tests green after. `npm test` green (80 tests), `tsc --noEmit`/`npm run build` clean. Fired Grade (`claude-haiku-4-5`, given only the DoD + diff, not the agent's own reasoning) as an independent check — PASS, no gaps. Fired Dream (`claude-opus-5`, given the full run history) since the Advise finding looked generalizable — it agreed and wrote `docs/RETRO.md` RETRO-2 (don't extend a TDD exemption to a new dependency by analogy/documentation alone; verify empirically in the target runtime first). No UI wiring (deliberately deferred to P2.5, per "no scope growth"); `DEFAULT_MODEL_URL` points at a browser path (`/basic-pitch/model/model.json`) that doesn't exist yet — P2.5 will need to actually copy the model's static assets somewhere servable. | On estimate (1 session, within the 2-session estimate) | P2.4 — `src/notes/clean.ts` (`filterShort`, `filterLowConfidence`, `mergeAdjacent`), TDD; note for P2.5: copy basic-pitch's model assets to a servable path matching `DEFAULT_MODEL_URL` |
| 2026-09-17 | 17 | P2.4 | Fixed a leftover duplicate P2.3 row in section 1's status table (artifact from a prior session) before starting. Branched `p2.4-notes-clean` off `main`. Per RETRO-1, checked `docs/PLAN.md` section 1/4 invariants before coding: the single-melody/one-note-at-a-time rule means `mergeAdjacent` must only ever merge same-midi notes, never bridge different pitches. Wrote `test/notes/clean.test.ts` red-first (20 tests): empty array, single note, exact-threshold boundaries (kept, i.e. `>=`) for `filterShort`/`filterLowConfidence`, same-`start` two-note merge, gap-at-threshold vs. gap-over-threshold for `mergeAdjacent`, different-midi non-merge, 3-note chain merge, no-mutation/order-preservation, and a `RawNote[]` case confirming the merged note keeps the first note's `confidence`. Implemented `src/notes/clean.ts` (`filterShort`, `filterLowConfidence`, generic `mergeAdjacent<T extends Note>` so it works on both `Note[]` and `RawNote[]`). First run had one failure from float imprecision (`1.1 - 1 !== 0.1` in JS floats) on the gap-at-threshold test — fixed with a small epsilon (`1e-9`) in the gap comparison, not by relaxing the test. `npm test` green (100 tests), `tsc --noEmit`/`npm run build` clean. No `[skill]`/Minh checkpoint — pure data functions, not wired into the UI yet. | On estimate (1 session, within the 1-session estimate) | P2.5 — wire file → decode → transcribe → clean → skyline, toggle play original/synth, playhead synced to original; also copy basic-pitch's model assets to a servable path (flagged at P2.3) |
| 2026-09-17 | 18 | — | No task-related code this session. Between sessions, PR #18 (P2.4) merged; also caught and fixed a process slip: a docs-only follow-up commit (parking a 60s-clip-cap idea Minh asked about) was pushed to `p2.4-notes-clean` *after* PR #18 had already merged, so it never reached `main`. First recovery attempt committed it directly to local `main`, which itself violated the branching rule — caught immediately (nothing pushed), reset `main` to `origin/main`, and re-did it properly as its own small branch + PR (#19, `docs-park-60s-cap`), still open. | — (recovery, not an estimated task) | PR #19 needs a merge decision from Minh (unrelated to P2.5's code, doesn't block it); then P2.5 |
| 2026-09-17 | 19 | P2.5 | Branched `p2.5-wire-transcription` off `main`. `scripts/copy-model.mjs` copies `@spotify/basic-pitch`'s model files (~900KB, not source) from `node_modules` to `public/basic-pitch/model/` (gitignored) via new `predev`/`prebuild` npm hooks, so `DEFAULT_MODEL_URL` (P2.3) is actually servable — flagged as an open gap at P2.3, closed here. Added one new pure function following the established `src/ui/` TDD precedent: `fitSkylineOptions(notes, canvasWidth, canvasHeight) → SkylineOptions` in `src/ui/geometry.ts`, written red-first in `test/ui/geometry.test.ts` (19 tests total in that file now) — fits a real transcribed melody's unknown pitch range/duration onto the fixed 700×200 canvas (padded midi range, floor 12-semitone span, clamped to piano range 21..108; pxPerSec = canvas width / total duration, clamped 5..200). Wired `main.ts`: the Import section's file handler now runs the full pipeline (`decodeFile` → `transcribe` → `filterLowConfidence` → `filterShort` → `mergeAdjacent`, fixed thresholds `minConfidence=0.3`/`minDur=0.05s`/`mergeGap=0.05s`) and loads the result into the same `notes`/skyline the hand-placed editor already uses, refitting `skylineOptions` via `fitSkylineOptions`; "Clear all" and the 3 presets reset `skylineOptions` back to the Phase 1 default (60–72 midi, 100 px/sec) so hand-editing isn't left at a squished real-song scale. Added "Play original audio" (builds an `AudioBuffer` from the decoded PCM directly, plays it, drives the same playhead loop as `playAll`) and "Play synth" (reuses the existing `playAll`) buttons — the DoD's "toggle" is 2 buttons on the same skyline rather than a single mode switch, since both are just alternate ways to trigger playback of what's already on screen. Hit one TS error (`filterLowConfidence` needs `RawNote[]`, but `filterShort`'s `Note[]` return type had already erased it) — fixed by reordering to `filterLowConfidence` before `filterShort`, not by changing either function's signature. `npm test` green (108 tests), `tsc --noEmit`/`npm run build` clean (prebuild's copy-model step runs automatically). Manually verified end-to-end in Chrome (no committed audio fixture — generated a synthetic 2s WAV, 440Hz then 880Hz, in the scratchpad dir, not committed): uploaded it, watched it decode → transcribe → clean → skyline in real time, got exactly 2 notes (short building then a taller one, matching the octave jump), pressed both "Play original audio" and "Play synth" with no console errors, then "Clear all" + click-to-add confirmed the hand-editor still works at the reset default scale. Left `npm run dev` running (port 5176) for Minh's checkpoint. **Not yet marking P2.5 `done`** — it's a `[skill]` task and the Phase 2 final checkpoint (a real ≤60s clip, Minh points to 1 matching and 1 non-matching spot) is Minh's call, not mine; status stays `doing` until he runs it and I record his words verbatim. | On estimate so far (within the 1–2 session estimate; session not yet closed — pending Minh's checkpoint) | Get Minh to run the Phase 2 final checkpoint with a real clip (e.g. the P2.2 YouTube wav or the P2.1 mp3), record his words verbatim in section 3, then mark P2.5 `done` and open its PR |
| 2026-09-17 | 20 | P2.5 | Minh reported BUG-4 (no way to pause/stop playback) while trying the P2.5 build; asked to fix it scoped to `playAll` (skyline's "Play"/"Play synth") and `playOriginal` ("Play original audio"), not the short single-note previews. `playNote` (`src/audio/synth.ts`) now returns its `OscillatorNode` instead of firing-and-forgetting it. Added shared `stopPlayback()` state in `main.ts` (`activeOscillators`, `activeSource`, `activeAnimationFrame`) that stops whatever's currently playing and cancels its playhead animation loop; `playAll`/`playOriginal` both call `stopPlayback()` on start (so starting one play action cuts off any other) and populate the active-node state. Added a "Stop" button next to the skyline's "Play" and another next to the Import section's play buttons, both wired to `stopPlayback`. `npm test` green (108 tests, unchanged — `[skill]` audio wiring, no pure function touched), `tsc --noEmit`/`npm run build` clean. Verified in Chrome via `npm run dev`: started the skyline's Play mid-melody, hit Stop, playhead vanished immediately with no console errors; same for "Play original audio" on the P2.5 synthetic-WAV test, including clicking Play then Stop back-to-back (stopping a source almost immediately after `start()` — no crash). Updated BUG-4's status in `docs/BUGS.md` to "decided, fixing in P2.5's branch". | Not separately estimated — folded into the still-open P2.5 session | Continue waiting on Minh's Phase 2 final checkpoint with a real clip |
| 2026-09-17 | 21 | P2.5 | Minh loaded a real 270.62s song ("LAVIEM.mp3", 2336 notes found) — "Play original audio" worked, "Play synth"/skyline "Play" produced no sound (logged as BUG-5). Investigated empirically rather than guessing: an `OfflineAudioContext` render with 200 heavily-overlapping notes through the same compressor setup produced clear signal (ruled out compressor/overlap as the cause); a real-time `AnalyserNode` tap showed 500 notes over 60s start producing sound within ~100ms, but 2336 notes over 270s stayed silent for ~900ms+ — pinning the cause on `playAll` creating one `OscillatorNode`/`GainNode` pair per note synchronously up front, which doesn't scale with note count. Reported this to Minh; he said the product needs to handle up to 5-minute inputs to be worth much, which reframed the earlier 60s-cap idea (parked at PR #19, still unmerged) — recorded as `docs/PLAN.md` DR-12: raise the target input length from ≤60s to ≤5min everywhere (Assumptions, section 5's ffmpeg command and clip-picking rule, the risk table, P2.5's own DoD checkpoint wording, `docs/input.md`, the Import section's UI copy), and fix `playAll` with a lookahead scheduler (`SCHEDULE_AHEAD_SEC = 2`, standard Web Audio pattern) instead of capping duration to route around the real scaling problem. Implemented: `scheduleDueEvents()` in `main.ts` walks the already-sorted `ScheduledEvent[]` from `schedule()` and only creates oscillators for events due within the next 2 seconds, called once up front and again every `tick()`. `npm test` green (108 tests, unchanged), `tsc --noEmit`/`npm run build` clean. Re-ran the same synthetic 2336-note/270s reproduction with the fix applied: first audible signal now at ~200ms (was ~900ms+), and only ~26 of 2336 notes were ever scheduled within the first 1.5s (was all 2336 up front) — confirmed via the browser, not assumed. Logged BUG-5 in `docs/BUGS.md` with both the investigation and the fix. **Note for whenever PR #19 (the parked 60s-cap idea) merges:** its `docs/LEDGER.md` section 6 row needs a follow-up edit pointing at DR-12 as the superseding decision — couldn't be done from this branch since #19 hasn't merged into `main` yet. | Not separately estimated — folded into the still-open P2.5 session | Reconcile PR #19's parked-idea row with DR-12 once #19 merges; continue waiting on Minh's Phase 2 final checkpoint with a real clip (now ≤5 min) |
| 2026-09-17 | 22 | P2.5 | Minh ran the Phase 2 final checkpoint with a real ~270s song ("LAVIEM.mp3"): sound now plays via "Play synth" (BUG-5 fix confirmed working), overall quality "khá tốt" (quite good) — matching spot; a few odd-sounding segments, which he attributed to genuine transcription behavior rather than a bug — non-matching spot, in line with `docs/PLAN.md` section 6's guidance that transcription noise on a real song is expected, not an app defect. Recorded verbatim in section 3. Phase 2 (P2.1–P2.5) is now fully done. | On/under estimate (2 sessions total including the BUG-4/BUG-5 investigation and fixes, within the 1–2 estimate's upper bound) | P3.1 (stretch, Phase 3) — editor works on transcribed notes, "Play synth of edited version" button; merge PR #20 once Minh gives the go-ahead, then `git checkout main && git pull` before branching |
| 2026-09-17 | 23 | P2.6 | Minh reported (outside a checkpoint) that loading a real song gives no feedback on whether the pipeline is running/hung/errored, and that the layout doesn't foreground the skyline. Logged both (BUG-6; a parked idea) first per protocol, rather than fixing inline. Minh asked to combine both into one task before P3.1, delegating the layout decision to the agent. Added task P2.6 to `docs/PLAN.md`'s Phase 2 table and DR-13 (canvas-first layout; real progress bar, not just elapsed-time text — checked `@spotify/basic-pitch`'s source first per RETRO-2 rather than assuming, found `evaluateModel` already takes a `percentCallback` that `src/transcribe/basicPitch.ts` was discarding as `() => {}`). Branched `p2.6-import-ux-polish` off `main`. Part (a), harness: added `onProgress?: (percent: number) => void` as `transcribe()`'s 3rd param, wired to Basic Pitch's existing callback; test written first in `test/transcribe/basicPitch.test.ts` (red — call count was 0 — then green), confirming values are non-decreasing and reach 1.0; existing 2 contract tests and return shape unchanged. Part (a) wiring in `main.ts`: a `setImportStage()` helper drives a spinner during decode (no progress hook available there), a real `<progress>` bar during transcribe (previously just static text), and a distinct red error-banner style on any caught exception; file input disabled while processing so a second import can't start mid-pipeline. Part (b): reflowed `index.html`'s markup (all still generated from `main.ts`) — skyline canvas moved above the header, enlarged 700×200 → 760×240 (`DEFAULT_SKYLINE_OPTIONS.canvasHeight` updated to match), header shrunk to a small top bar; hand-editor hints/Play C4-C5 moved into a new "Build by hand" panel below the import section; `.wrap` widened 760px → 820px. All element ids kept identical, so no JS wiring changed beyond the import-status helper. Caught one real bug via browser verification before showing Minh: the `.import-progress`'s `display: block` rule overrode its `hidden` attribute, so the progress bar showed even when idle — fixed with an explicit `.import-progress[hidden] { display: none; }` rule. Verified end-to-end in Chrome with 3 synthetic clips generated in the scratchpad dir (not committed): a 3s clip (fast path, spinner then immediate "done"), a 40s clip (caught the progress bar moving 20% → 40% → 72% → done, confirming the percentage is real, not decorative), and a deliberately-corrupt file (confirmed the red error banner and that Play buttons correctly stayed disabled). `npm test` green (109 tests), `tsc --noEmit`/`npm run build` clean. Minh's checkpoint: part (a) passed clean ("Ok rồi đó, có 1 thanh progress khá trực quan, mặc dù nó xấu" — works, intuitive, though ugly); part (b) passed with a caveat ("Tạm ổn rồi, nhưng chắc là phải redesign lại nữa, hiện tại chưa có idea lắm" — acceptable for now, likely wants another redesign pass, no concrete direction yet) — both recorded verbatim in section 3, both logged as new parked ideas (progress-bar visual polish; a further layout redesign, not yet scoped pending Minh's direction) rather than treated as DoD failures, since the DoD asked whether the skyline reads as the main focus (yes) not whether the design is finished. | On estimate (1 session) | Merge PR once opened and Minh gives the go-ahead, then `git checkout main && git pull`; next up is P3.1 (stretch, Phase 3) unless Minh wants the parked redesign/progress-bar-polish ideas scoped first |

---

## 3. Minh's words at checkpoints

Verbatim, short. This is learning evidence, not code evidence.

| Date | Checkpoint | Minh said |
|---|---|---|
| 2026-09-12 | P1.3 (2 notes, 12 steps apart) | "C5 is higher" (chose the C5 button as higher-pitched than C4) |
| 2026-09-12 | P1.4 (point to the tallest building) | "4th building" (picked correctly before playback), then confirmed "Yes, it matched" after listening |
| 2026-09-12 | P1.5 (drag 1 step vs 12 steps) | "Kéo lên nấc cao nhất, nghe như tiếng è è của điện thoại, rất khó chịu." (12 steps, top of range) — "Kéo lên 1 nấc thôi, không khó chịu như vậy." (1 step) |
| 2026-09-12 | P1.8 (hover a few buildings, label reads naturally) | "Có, chính xác rồi." |
| 2026-09-12 | P1.6 (build own 8 notes, sounds okay + explain 1 choice) | "Nghe cũng ổn, đôi khi phát âm thanh bị rè." (sounds okay; also reported crackling audio, logged as BUGS.md BUG-1) — on choosing the notes: "tôi click đại đại thôi, và cảm thấy khó khăn khi quyết định nốt." (clicked mostly at random, found it hard to decide) |
| 2026-09-12 | P1.9 (3 presets, pleasant/recognizable) | "Các bài hát rất chính xác, nghe rất quen thuộc." |
| 2026-09-12 | P1.7 (considered design, not default Vite look) | On the night-skyline redesign: "cửa sổ nhìn cảm giác không ra cửa sổ. Và nền đen khiến nó trông không giống thành phố lắm" → iterated (window grid + sky gradient + ground strip) → "bầu trời trông không đẹp lắm, nhưng mà đẹp hơn lúc trước rồi" → asked to park further sky polish and close the checkpoint at the current state: "để dành làm sau, chốt checkpoint P1.7 ở mức hiện tại." Also found BUG-2 (right-edge clipping) during this checkpoint, confirmed as still present, decided to fix as its own task right after P1.7. On every existing control still working: "đúng, mọi thứ vẫn hoạt động bình thường." |
| 2026-09-12 | P1.10 (place/stretch a note near the right edge) | "ok rồi" |
| 2026-09-17 | P1.11 (micro-feedback, undo after Clear all, edge stop-cue) | "it works well." |
| 2026-09-17 | P2.2 (follow docs/input.md, get a wav file from a YouTube link) | "File này mở và nghe bình thường. Document khá tốt, tôi có rõ không không quan trọng, quan trọng là agent đã thực hiện thành công." (file opens/plays fine; doc is fairly good; whether *he* personally understands it doesn't matter, what matters is the agent executed it successfully) |
| 2026-09-17 | P2.5 (skyline vs ear: matching and non-matching spots, real ~270s song "LAVIEM.mp3") | "có âm thanh rồi, khá tốt, có 1 vài đoạn âm thanh hơi lạ nhưng có vẻ là nốt gốc nó như vậy." (sound plays now, quite good overall — matching; a few segments sound a bit odd, but that seems to be how the original transcribed notes actually are, not a bug — non-matching, attributed to real transcription accuracy per `docs/PLAN.md` section 6's "reading Phase 2 results" guidance) |
| 2026-09-17 | P2.6a (import progress: can Minh tell decoding/transcribing/done/failed at every moment) | "Ok rồi đó, có 1 thanh progress khá trực quan, mặc dù nó xấu." (works now, the progress bar is quite intuitive, though it looks ugly) |
| 2026-09-17 | P2.6b (does the skyline read as the main focus over the header/Play C4/C5 buttons) | "Tạm ổn rồi, nhưng chắc là phải redesign lại nữa, hiện tại chưa có idea lắm." (acceptable for now, but will likely need another redesign pass; no concrete idea yet) |

---

## 4. End-of-phase review

Fill in at the end of each phase, or when a trigger in docs/PLAN.md section 9 fires.

| Phase | Total est. | Total actual | Main cause of variance | Adjustment for next phase |
|---|---|---|---|---|
| Phase 1 | 6.5–7.5 (+1.5 contingency), extended to ~10.5–11.5 after P1.7/P1.8/P1.9/P1.10 were added mid-course | 10 (sum of each task's "Actual") | Estimate held well for the originally-planned P1.1–P1.6; four additional tasks (P1.7 redesign, P1.8 solfège labels, P1.9 example melodies, P1.10 bug fix) were added mid-course at Minh's request, all in-checkpoint discoveries rather than scope creep from the agent. BUG-2 (right-edge clipping) surfaced during both P1.6 and P1.7 checkpoints, was deliberately deferred per the bug-log process each time, then fixed as its own task once Minh decided it should be prioritized. | Phase 2 keeps the same ~20–30% contingency; expect at least one more mid-course task addition given the pattern in Phase 1 (checkpoints keep surfacing real UX gaps and defects, not just confirming the DoD). |
| Phase 2 | 5.5–7.5 (+2 contingency) | | | |

---

## 5. Open questions answered

From docs/PLAN.md section 11. Record the answer, keep the same numbering.

| Q | Answer | Date |
|---|---|---|
| Q1 | | |
| Q2 | | |
| Q3 | Solfège (Đô Rê Mi Fa Sol La Si), fixed-do, as the default label. Minh only knows solfège, not letter names. Recorded as DR-8, built as task P1.8. | 2026-09-12 |

---

## 6. Parked ideas

Ideas that come up mid-work. Record here, do not build now. Review at the end of each phase.

| Date | Idea | From | Decision |
|---|---|---|---|
| 2026-09-12 | Constrain the editor to a musical scale (e.g. only diatonic notes of a chosen key), instead of free chromatic (any of 12 semitones) placement. | Minh, during P1.5 checkpoint | Not now. Recommended keeping free chromatic for v1 — Phase 1's goal is basic "higher/lower" understanding, not scale theory; scale-constrained placement adds a new concept (key/scale selection) and reduces experimentation freedom. Revisit after Phase 1 if Minh still wants it. |
| 2026-09-12 | Fixed slot grid (e.g. 8 equal-width cells, one note each, can be empty) instead of free click-to-place + drag-to-resize duration. | Minh, during P1.6 checkpoint (clicking imprecisely left gaps) | Not now. Recommended keeping free duration — note-length variation is rhythm, a core part of melody; a same-width grid would feel more like a scale exercise than composing. The actual pain point (imprecise clicks leaving gaps) is a snap-grid granularity issue, not a duration-freedom issue; if it recurs, coarsen the existing time snap (`TIME_STEP` in `main.ts`, currently 0.05s) instead of removing duration freedom. |
| 2026-09-12 | Further polish the skyline's sky background (Minh: "bầu trời trông không đẹp lắm, nhưng mà đẹp hơn lúc trước rồi") — gradient/ground styling in `src/ui/skyline.ts` is an improvement over flat black but not fully satisfying yet. | Minh, during P1.7 checkpoint | Parked at Minh's request ("để dành làm sau, chốt checkpoint P1.7 ở mức hiện tại"). Revisit with specific direction from Minh on what reads wrong (color, gradient, overall feel) before iterating further. |
| 2026-09-13 | Instant micro-feedback on every playground interaction (visual "pop" on note placement before sound plays, live pitch/duration readout while dragging) — reduces ambiguity about whether a click/drag registered. | Agent brainstorm, post-P1.10 | Scoped into task P1.11 (2026-09-14, DR-10) at Minh's request. |
| 2026-09-13 | Cheap in-memory undo (last-state stack) for add/drag/delete/"Clear all" — reduces hesitation to experiment since "Clear all" is currently one-way. | Agent brainstorm, post-P1.10 | Scoped into task P1.11 (2026-09-14, DR-10) at Minh's request. |
| 2026-09-17 | Enforce the ≤ 60s clip-length recommendation in-app (progress bar and/or a warning/auto-cap) instead of leaving it as a manual ffmpeg/clip-picking step — right now nothing in the code stops a >60s file from being decoded and sent through transcription, which is slow and noisier per `docs/PLAN.md` section 5/8's "Cap at 60s" fallback. | Minh, asking what happens with a >60s video | **Superseded (2026-09-17) by `docs/PLAN.md` DR-12**: P2.5's checkpoint did surface exactly the slowness this idea anticipated (BUG-5 — no sound on a real ~270s clip, root-caused to `playAll` scheduling all notes up front rather than transcription itself being slow), but Minh's resolution was the opposite of capping — he decided the product needs to handle up to 5 minutes to be worth much, so the target length was raised (~60s → ~5min) and the actual bottleneck (playback scheduling) was fixed instead. A future progress bar for long transcriptions is still a legitimate idea, but no longer as a length *cap*. |
| 2026-09-13 | Visible stop-cue (e.g. subtle edge highlight) when a drag hits the canvas boundary, so the P1.10 clamp reads as intentional rather than a silent bug. | Agent brainstorm, post-P1.10 | Scoped into task P1.11 (2026-09-14, DR-10) at Minh's request. |
| 2026-09-17 | UI layout doesn't foreground the skyline/editor (the app's main feature) — Minh feels it reads as unfocused. Too vague yet to scope: which element(s) compete for attention, what should be more prominent, etc. not yet specified. | Minh, unprompted (not during a task checkpoint) | Superseded by P2.6 (2026-09-17, DR-13): reordered/enlarged the skyline as the page's first and largest element. Minh's P2.6 checkpoint said this is "tạm ổn" (acceptable for now) but flagged he'll likely want another redesign pass — no concrete direction yet. New row below captures that follow-up. |
| 2026-09-17 | Another redesign pass on the overall page layout, beyond P2.6's canvas-first reorder. No concrete direction yet — Minh has no specific idea of what still reads wrong. | Minh, during the P2.6 checkpoint | Not scoped. Needs a follow-up conversation with Minh on what specifically still feels off before this becomes a task — don't guess at a direction without his input, per DR-13's own "why not remove/redesign further without a task asking for it" reasoning. |
| 2026-09-17 | Visual polish on the import progress bar (native `<progress>` styling) — Minh said it's functionally clear ("khá trực quan") but "xấu" (ugly). | Minh, during the P2.6 checkpoint | Not scoped. Purely cosmetic, functionality already confirmed working — low priority, revisit alongside the next design pass rather than as its own task. |

---

## 7. Incidents and fixes

Environment, codec, library problems. Record so they are not repeated.

| Date | Incident | Fix | Related task |
|---|---|---|---|
| | | | |
